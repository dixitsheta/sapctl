{
  "_comment": "Drop into ~/.cursor/mcp.json (global) or <project>/.cursor/mcp.json (per-project). Restart Cursor after editing.",
  "mcpServers": {
    "sapctl": {
      "command": "/usr/local/bin/sapctl",
      "args": ["mcp", "serve"],
      "env": {
        "HOME": "${HOME}",
        "XDG_CONFIG_HOME": "${HOME}/.config",
        "PATH": "/usr/local/bin:/usr/bin:/bin"
      }
    }
  }
}
